<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tk.takenoteapp">

    <!-- internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecordVoice" />
        <activity android:name=".ShowNotes" />
        <activity android:name=".TakePhoto" />
        <activity android:name=".TextNote" />
        <activity android:name=".ShowAllNotes" />
        <activity android:name=".WelcomePage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".SignupPage" />
        <activity android:name=".AfterLogMainPage" />
        <activity android:name=".TagsPage" />
        <activity android:name=".InboxPage"></activity>
    </application>

</manifest>